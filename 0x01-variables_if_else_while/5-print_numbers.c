#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	unsigned short int x = 0;

	for (x = 48; x <= 57; x++)
	{
		putchar(x);
	}
	printf("\n");
return (0);
}
